# -*- mode: shell-script -*-
# Defaults for nodejs-harrison initscript
# sourced by /etc/init.d/nodejs-harrison-web
# installed at /etc/default/nodejs-harrison by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""

# TODO specify web interface port

# Change this to `yes' to run this daemon
RUN=no
